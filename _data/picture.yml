presets:
  atf-large:
    markup: direct_url
    formats: [webp, avif]
    fallback_width: 1600        
    fallback_format: webp
  atf-mobile:
    markup: direct_url
    formats: [avif, webp]
    fallback_widths: 450    
    fallback_format: webp
  direct:
    markup: direct_url
    formats: [avif, webp]
    fallback_width: 1200
    fallback_format: webp
  nav-logo:
    markup: direct_url
    formats: [avif, webp]
    fallback_width: 250
    fallback_format: webp
  preview:
    markup: direct_url   
    formats: [avif, webp]     
    fallback_width: 600
    fallback_format: webp
  only_srcset:
    markup: naked_srcset
    widths: [360, 800, 1200, 1600]
    formats: [avif, webp]
  small_logo:
    markup: picture
    formats: [avif, webp]
    widths: [200, 360]
  atf-xxlarge:
    markup: picture
    formats: [webp]
    widths: [1600, 1024, 768, 500]
  xxlarge:
    markup: picture
    formats: [webp]
    widths: [1000, 800, 450]
  xlarge:
    markup: picture
    formats: [avif, webp]
    widths: [600, 450, 250]
  large:
    markup: picture
    formats: [avif, webp]
    widths: [500, 250, 150]
  medium:
    markup: picture
    formats: [avif, webp]
    widths: [350]
  small:
    markup: picture
    formats: [avif, webp]
    widths: [200]
  xsmall:
    markup: picture
    formats: [avif, webp]
    widths: [150]  
  thumb:
    markup: picture
    formats: [avif, webp]
    widths: [70]
  category-post:
    markup: picture
    formats: [webp]
    widths: [600, 450, 250]
    fallback_format: webp
  blog-feature:
    markup: direct_url
    formats: [webp]
    widths: [1200, 1000]  
  blog-preview:
    markup: direct_url
    formats: [avif, webp]
    widths: [750, 600, 450]
    fallback_format: webp
    fallback_width: 450    